# Design Blueprint — {{brand.name}}

> {{brand.tagline}}

**Source:** {{source_url}}
**Extracted:** {{extraction_date}} via `scripts/extract.sh` (Firecrawl /v2/scrape) + `scripts/css-tokens.sh` (verbatim tokens)
**Slug:** `{{slug}}`

## Positioning

{{brand.positioning}}

---

## 1. Color Palette

| Role | Value |
|---|---|
| **Primary** | `{{color_palette.primary}}` |
| **Secondary** | {{color_palette.secondary as comma list of hex}} |
| **Accent** | {{color_palette.accent as comma list of hex}} |
| Background | `{{color_palette.neutrals.background}}` |
| Surface | `{{color_palette.neutrals.surface}}` |
| Text (primary) | `{{color_palette.neutrals.text_primary}}` |
| Text (muted) | `{{color_palette.neutrals.text_muted}}` |
| Border | `{{color_palette.neutrals.border}}` |

{{#if color_palette.gradients.length}}
**Gradients:**
{{#each color_palette.gradients}}
- {{this}}
{{/each}}
{{/if}}

---

## 2. Typography

**Heading:** `{{typography.heading_font.family}}` — {{typography.heading_font.feel}}
Weights used: {{typography.heading_font.weights_used as comma list}}

**Body:** `{{typography.body_font.family}}` — {{typography.body_font.feel}}
Weights used: {{typography.body_font.weights_used as comma list}}

**Scale relationship:** {{typography.scale_relationship}}

---

## 3. Layout Vibe

- **Density:** {{layout_vibe.density}}
- **Grid:** {{layout_vibe.grid}}
- **Hero pattern:** {{layout_vibe.hero_pattern}}
- **Section rhythm:** {{layout_vibe.section_rhythm}}
- **Imagery style:** {{layout_vibe.imagery_style}}

---

## 4. Motion & Interaction

- **Scroll behavior:** {{motion_and_interaction.scroll_behavior}}
- **Hover states:** {{motion_and_interaction.hover_states}}
- **Transitions:** {{motion_and_interaction.transitions}}

{{#if motion_and_interaction.notable_micro_interactions.length}}
**Notable micro-interactions:**
{{#each motion_and_interaction.notable_micro_interactions}}
- {{this}}
{{/each}}
{{/if}}

---

## 5. Copy Tone

- **Voice:** {{copy_tone.voice}}
- **Sentence length:** {{copy_tone.sentence_length}}
- **Jargon level:** {{copy_tone.jargon_level}}

**Headlines from the page:**
{{#each copy_tone.headline_examples}}
- *"{{this}}"*
{{/each}}

**CTAs from the page:**
{{#each copy_tone.cta_examples}}
- `{{this}}`
{{/each}}

---

## 6. Verbatim design tokens

{{paste the full stdout of scripts/css-tokens.sh here — the deterministic capture: brand custom properties, @font-face families, font stacks, font-variation-settings, radii, shadows, letter-spacing, load-bearing rules. If the script found nothing, keep its honest-empty output so the reader knows the token layer is missing, not omitted.}}

---

## What makes this distinctive

{{#each what_makes_it_distinctive}}
- {{this}}
{{/each}}

## What to steal

{{#each what_to_steal}}
- {{this}}
{{/each}}

## What NOT to steal

{{#each do_not_steal}}
- {{this}}
{{/each}}

---

## How to use this blueprint

Pick the next workflow (mirrors SKILL.md Step 5):

- **One-shot rebuild ("levels this up")** — build a NEW original site FROM this blueprint: obey its tokens/type/rhythm, invented placeholder brand, out-execute the reference (never copy), gate through `scripts/frontend-review.sh`.
- **`/frontend-design`** — feed this blueprint into the Frontend Design skill to render a polished one-page HTML mock.
- **`/gsd-ui-phase`** — turn this into a UI-SPEC.md design contract for a planned phase.

Manual alternatives outside the Step-5 flow: `/gsd-sketch` (throwaway visual exploration) · Claude Design (Anthropic Labs web app, brand-from-zero) · paste this whole file into a fresh chat with *"Build me a landing page using this design blueprint, but for [your product]."*

---

*Generated by `/design-blueprint` v2. Doctrine: `references/website-design-levels.md` (Level 7) + `references/claude-design.md` "Jack Roberts angle". Engines: `scripts/extract.sh` (Firecrawl /v2/scrape) + `scripts/css-tokens.sh` (verbatim tokens).*
